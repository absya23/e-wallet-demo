import React from "react";
import { Text, View } from "react-native";

const TransactionHistory = ({ params }) => (
	<View>
		<Text>TransactionHistory</Text>
	</View>
);

export default TransactionHistory;
